SELECT 
    MD_UPD_ACTUAL_BATCH_ID,
    MD_UPD_INITIAL_BATCH_ID,
    MD_INS_ACTUAL_BATCH_ID,
    MD_INS_INITIAL_BATCH_ID,
    FEATURES_B,
    FEATURES_A,
    NIM_SERVICE_ID,
    SRVCE_DIR_ENT_DTL_ID,
    SRVCE_EMRG_SVC_DTL_ID,
    MEDIA_BOX_ID,
    SWITCH_SERVICE_ID,
    WLR3_DQ_DETAILS_ID,
    PRODUCT_SPEC_ID,
    SERVICE_ID,
    VOICE_SERVICE_KEY,
    DL_SPEED,
    UL_SPEED,
    ENDPOINT_ID,UAN,
    CIRCUIT_REFERENCE_NUMBER,
    CABLE_LINK_REFERENCE,
    POSTCODE,
    SERVICE_TYPE,
    EXCHANGE_CODE,
    DIRECTORY_NUMBER,
    ENDPOINT_REFERENCE,
    BT_CIRCUIT_ID,
    HECTOR_SERVICE_RFRNCE,
    RESTRICTION_TYPE,
    EXTRNL_PROVIDER_SERVICE_REF,
    OPERATOR,
    SERVICE_SPEC_CODE,
    MD_UPD_TS,
    MD_INS_TS,
    MD_EFFCTV_TO_TS,
    MD_EFFCTV_FROM_TS,
    CREATED_DTM,
    DN_START_DATE,
    BT_CEASE_WARNING_DATE,
    NED_CEASE_WARNING_DATE,
    UNSLCTD_CEASE_WARNING_DATE,
    SERVICE_CEASE_DATE,
    SERVICE_START_DATE,
    ACTIVATED,
    MIGRATING,
    CAST(NULL as VARCHAR(10)) AS CARE_LEVEL,
    MD_CURR_FLAG
FROM ODW_VOICE_SERVICE_DIM
WHERE (
    MD_INS_TS >= '{from_ts}'
    AND MD_INS_TS < '{to_ts}'
) OR (
    MD_UPD_TS >= '{from_ts}'
    AND MD_UPD_TS < '{to_ts}'
);
